package com.example.aldegardproject.models.cards;

public class MilitaryCard extends Card{
    public MilitaryCard(String title, String description, int[] bonus, int optimalValue, boolean isConstantCard) {
        super(title, description, bonus, optimalValue, isConstantCard);
    }
}
