package com.example.aldegardproject.models.cards;

public class SatisfactionCard extends Card{
    public SatisfactionCard(String title, String description, int[] bonus, int optimalValue, boolean isConstantCard) {
        super(title, description, bonus, optimalValue, isConstantCard);
    }
}
