package com.example.aldegardproject.models.cards;

public class FaithCard extends Card{

    public FaithCard(String title, String description, int[] bonus, int optimalValue, boolean isConstantCard) {
        super(title, description, bonus, optimalValue, isConstantCard);
    }
}
